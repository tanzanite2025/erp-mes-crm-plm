# 供应商编号自动生成修复方案

## 📋 问题总结

### 原始问题
1. **错误提示不明确**：后端返回 `code and name must not be empty`，但没有说明具体是哪个字段、是完全为空还是只有空格
2. **编号必填不合理**：供应商编号要求用户手动填写，但应该由系统自动生成
3. **空格处理**：后端使用 `strings.TrimSpace()` 处理，只有空格的输入会被视为空

### 根本原因
- 后端验证逻辑：`if model.Name == "" || model.Code == "" { return error }`
- 前端没有验证，允许空值或纯空格提交
- 编号字段设计不合理，应该自动生成而非手动输入

## ✅ 实施的解决方案

### 1. 前端改进

#### A. 表单验证增强 (`supplier-action-dialog.tsx`)
```typescript
const handleSave = () => {
  // Trim whitespace from required fields
  const trimmedName = nextData.name?.trim() || ''
  const trimmedCode = nextData.code?.trim() || ''
  
  // Validate name (always required)
  if (!trimmedName) {
    toast.error(t('purchase.suppliers.validation.nameRequired'))
    return
  }
  
  // For new suppliers, code can be empty (auto-generated by backend)
  // For existing suppliers, code must not be empty
  if (isPatch && !trimmedCode) {
    toast.error(t('purchase.suppliers.validation.codeRequired'))
    return
  }
  
  // Update with trimmed values
  nextData.name = trimmedName
  nextData.code = trimmedCode
}
```

**改进点**：
- ✅ 自动去除前后空格
- ✅ 明确区分新建和编辑场景
- ✅ 新建时编号可为空（由后端生成）
- ✅ 编辑时编号不能为空
- ✅ 提供清晰的错误提示

#### B. UI 改进
```tsx
// 编号字段：新建时禁用，编辑时必填
<Label htmlFor='code'>
  {t('purchase.suppliers.fields.code')} 
  {supplier && <span className='text-destructive'>*</span>}
</Label>
<Input
  id='code'
  placeholder={supplier ? '例如：XD-S-2024XX' : '保存后自动生成'}
  disabled={!supplier}  // 新建时禁用
  value={formData.code}
  onChange={(e) => updateField('code', e.target.value)}
/>
```

**改进点**：
- ✅ 新建时编号字段禁用，显示"保存后自动生成"
- ✅ 编辑时编号字段可编辑，标记为必填
- ✅ 视觉上清晰区分两种模式

#### C. 翻译文件更新

**中文** (`zh-CN/suppliers.ts`):
```typescript
fields: {
  code: '厂商编号',
  codePlaceholder: '例如：XD-S-2024XX',
  codeAutoPlaceholder: '保存后自动生成',  // 新增
},
validation: {
  nameRequired: '厂商名称为必填项，请填写后再保存',  // 新增
  codeRequired: '厂商编号不能为空',  // 新增
}
```

**英文** (`en-US/suppliers.ts`):
```typescript
fields: {
  code: 'Vendor Code',
  codePlaceholder: 'e.g. XD-S-2024XX',
  codeAutoPlaceholder: 'Auto-generated after saving',  // 新增
},
validation: {
  nameRequired: 'Vendor name is required. Please fill it in before saving.',  // 新增
  codeRequired: 'Vendor code cannot be empty',  // 新增
}
```

### 2. 后端改进

#### A. 自动生成编号逻辑 (`partner_service.go`)

```go
func SaveSupplier(input SaveSupplierRequest, actorID string, operator string, ip string) (SupplierResponse, error) {
	model := MapSaveSupplierRequestToModel(input)
	model.Name = strings.TrimSpace(model.Name)
	model.Code = strings.TrimSpace(model.Code)
	
	// Name is always required
	if model.Name == "" {
		return SupplierResponse{}, fmt.Errorf("%w: name must not be empty", ErrSupplierTransactionInvalidPayload)
	}
	
	// For new suppliers (no ID), auto-generate code if empty
	if strings.TrimSpace(model.ID) == "" {
		if model.Code == "" {
			model.Code = generateSupplierCode()  // 自动生成
		}
		model.Version = 1
		if err := db.DB.Create(&model).Error; err != nil {
			return SupplierResponse{}, err
		}
		syncSupplierToSearch(model)
		return MapSupplierToResponse(model), nil
	}
	
	// For existing suppliers, code must not be empty
	if model.Code == "" {
		return SupplierResponse{}, fmt.Errorf("%w: code must not be empty for existing supplier", ErrSupplierTransactionInvalidPayload)
	}
	
	// ... rest of the code
}
```

#### B. 编号生成器实现

```go
// generateSupplierCode generates a unique supplier code
// Format: XD-S-YYYYMMDD-NNNN where NNNN is a sequential number
func generateSupplierCode() string {
	now := time.Now()
	datePrefix := now.Format("20060102")  // 例如：20240514
	
	var maxCode string
	db.DB.Model(&models.Supplier{}).
		Where("code LIKE ?", fmt.Sprintf("XD-S-%s-%%", datePrefix)).
		Order("code DESC").
		Limit(1).
		Pluck("code", &maxCode)
	
	sequence := 1
	if maxCode != "" {
		// Extract sequence number from code like "XD-S-20240514-0001"
		parts := strings.Split(maxCode, "-")
		if len(parts) == 4 {
			if seq, err := strconv.Atoi(parts[3]); err == nil {
				sequence = seq + 1
			}
		}
	}
	
	return fmt.Sprintf("XD-S-%s-%04d", datePrefix, sequence)
}
```

**编号格式**：`XD-S-YYYYMMDD-NNNN`
- `XD-S`: 固定前缀（纤镀-供应商）
- `YYYYMMDD`: 创建日期（例如：20240514）
- `NNNN`: 当天的流水号（0001-9999）

**示例**：
- `XD-S-20240514-0001` - 2024年5月14日第1个供应商
- `XD-S-20240514-0002` - 2024年5月14日第2个供应商
- `XD-S-20240515-0001` - 2024年5月15日第1个供应商

## 🎯 修复效果对比

### 修复前
| 场景 | 行为 | 问题 |
|------|------|------|
| 新建供应商，不填编号 | ❌ 后端报错 | 用户必须手动填写编号 |
| 新建供应商，只填空格 | ❌ 后端报错 | 错误提示不明确 |
| 编辑供应商，清空编号 | ❌ 后端报错 | 可能误操作 |

### 修复后
| 场景 | 行为 | 改进 |
|------|------|------|
| 新建供应商，不填编号 | ✅ 自动生成 | 用户体验更好 |
| 新建供应商，只填空格 | ✅ 前端验证 | 明确提示"名称为必填项" |
| 编辑供应商，编号字段禁用 | ✅ 无法清空 | 防止误操作 |
| 编辑供应商，清空名称 | ✅ 前端验证 | 明确提示"名称为必填项" |

## 📝 测试建议

### 前端测试
1. **新建供应商**
   - [ ] 不填任何内容，点击保存 → 应提示"名称为必填项"
   - [ ] 只填名称，点击保存 → 应成功，编号自动生成
   - [ ] 名称只填空格，点击保存 → 应提示"名称为必填项"
   - [ ] 编号字段应该是禁用状态，显示"保存后自动生成"

2. **编辑供应商**
   - [ ] 编号字段应该可编辑，显示必填标记 *
   - [ ] 清空名称，点击保存 → 应提示"名称为必填项"
   - [ ] 清空编号，点击保存 → 应提示"编号不能为空"

### 后端测试
1. **编号生成**
   - [ ] 创建第一个供应商 → 编号应为 `XD-S-YYYYMMDD-0001`
   - [ ] 同一天创建第二个 → 编号应为 `XD-S-YYYYMMDD-0002`
   - [ ] 第二天创建 → 编号应为 `XD-S-YYYYMMDD-0001`（重新从1开始）

2. **并发测试**
   - [ ] 同时创建多个供应商 → 编号不应重复

## 🔄 后续优化建议

1. **编号生成策略**
   - 考虑使用数据库序列或分布式ID生成器（如雪花算法）
   - 添加事务锁防止并发时编号重复

2. **错误提示优化**
   - 后端返回更详细的错误信息（字段级别）
   - 前端显示字段级别的错误提示

3. **编号格式配置化**
   - 允许管理员自定义编号格式
   - 支持不同类型供应商使用不同前缀

4. **审计日志**
   - 记录编号生成历史
   - 追踪编号变更记录

## 📚 相关文件

### 前端
- `src/features/trading/components/supplier-action-dialog.tsx` - 表单组件
- `src/features/trading/hooks/use-supplier-action-view-model.ts` - 视图模型
- `src/locales/overrides/purchase/zh-CN/suppliers.ts` - 中文翻译
- `src/locales/overrides/purchase/en-US/suppliers.ts` - 英文翻译

### 后端
- `server/services/partner_service.go` - 供应商服务
- `server/services/partner_transaction_service.go` - 事务处理
- `server/handlers/suppliers.go` - HTTP 处理器

## ✨ 总结

这次修复解决了三个核心问题：
1. ✅ **编号自动生成** - 新建时无需手动填写
2. ✅ **验证逻辑优化** - 前端验证 + 明确错误提示
3. ✅ **用户体验改善** - 清晰的UI状态和提示信息

修复后的系统更加智能和用户友好，减少了用户的操作负担和出错可能性。
