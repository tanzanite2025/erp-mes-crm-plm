- [ ] 723 production-shared 机器码字段统一收口规划（中文），记录 ProductionLine.code / ProductionProcessStep.code 的排查结论、推荐实施范围与待确认执行项。
  - [x] 已确认本轮最明确的字段包括：
    - [x] `ProductionLine.code`
    - [x] `ProductionProcessStep.code`
  - [x] 已确认当前并非完全无规则，而是已经存在 production-shared 域内 helper：
    - [x] `src/features/production-shared/utils/production-code-normalization.ts` 已提供 `normalizeProductionLineCode / normalizeProductionProcessStepCode`
    - [x] 同文件已提供 `normalizeProductionLineEntity / normalizeProductionProcessStepEntity`
  - [x] 已确认当前真实问题不是“没有 helper”，而是“边界仍然局部散落”：
    - [x] `production-lines-service.ts` 保存边界已复用 entity helper
    - [x] `production-processes-service.ts` 保存边界已复用 entity helper
    - [x] `production-resource-api-adapter.ts` DTO 边界已复用 code helper
    - [x] `line-dialog.tsx` 输入/自动生成编号边界仍在局部处理
    - [x] `process-library-panel.tsx` 提交前仍直接散落调用 `normalizeProductionProcessStepCode / normalizeProductionProcessStepEntity`
  - [x] 已确认本轮推荐方向：
    - [x] 保持 `production-code-normalization.ts` 作为单一入口
    - [x] 收口 line / process 的输入边界、提交前 payload 组装边界、service 保存边界、adapter DTO 边界
    - [x] 去掉页面层重复散落的 code 规范化调用
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/production-shared/utils/production-code-normalization.ts`
    - [x] `src/features/production-shared/tabs/line-mgmt/components/line-dialog.tsx`
    - [x] `src/features/production-shared/tabs/work-architecture/components/process-library-panel.tsx`
    - [x] `src/features/production-shared/services/production-lines-service.ts`
    - [x] `src/features/production-shared/services/production-processes-service.ts`
    - [x] `src/features/production-shared/adapters/production-resource-api-adapter.ts`
  - [x] 已确认本轮非目标边界：
    - [x] 不扩到 `segments / jobCategories / attributes`
    - [x] 不重做 line / process 页面 UI 结构
    - [x] 不顺手改无关 version / authCode / delta 逻辑
    - [x] 不处理无关样式 warning
  - [ ] 待你确认后执行下一阶段：
    - [ ] 收口 line / process 的输入边界与提交前 payload 组装边界
    - [ ] 复核并最小收紧 service / adapter 的统一 helper 复用口径
    - [ ] 执行定向 TypeScript 校验并更新 `walkthrough.md`

- [ ] 724 产品型号编辑弹窗规格模板不回显排查与修复规划（中文），记录回显链路、根因假设、修复范围与待确认执行项。
  - [x] 已确认问题现象：
    - [x] 已建立的产品型号点击编辑后，弹窗顶部“规格模板”输入框为空
    - [x] 弹窗中部仅显示“待选择规格模板”占位态，未渲染模板表单
    - [x] 产品模板管理页中模板数据本身存在，且界面无报错提示
  - [x] 已确认当前前端模板回显链路：
    - [x] `ProductPartsMgmt` 通过 `useProductMgmt()` 将 `productTypes` 传入 `ProductActionDialog`
    - [x] `ProductActionDialog` 基于表单中的 `typeId` 从 `productTypes` 查找当前分类
    - [x] 仅当命中的 `selectedType.templateId` 存在时，才会调用 `getEffectiveTemplate(...)` 解析模板并渲染规格表单
    - [x] `ProductBasicInfo` 顶部“规格模板”输入框展示的也是解析后的 `templateLabel`
  - [x] 已确认当前真实风险点：
    - [x] 该问题不是模板页无数据，而是编辑弹窗上下文中的分类对象未能正确提供模板绑定信息时，会静默退化为“无模板”状态
    - [x] `ProductActionDialog` 对 `selectedType.templateId` 为空的场景直接清空 `boundTemplate`，不会提示异常
    - [x] 因此前端只要拿到的是“缺少 `templateId` 的分类对象”，即使模板页存在数据，也不会显示规格模板
  - [x] 已确认本轮优先核查与修复边界：
    - [x] 核查 `productTypes` 查询返回到弹窗时是否完整保留 `templateId`
    - [x] 核查是否存在 options/list DTO、归一化或缓存环节把 `templateId` 裁掉
    - [x] 在不改变模板页管理逻辑的前提下，修复产品编辑弹窗的模板绑定解析
    - [x] 视情况补充最小化容错，避免再次静默退化为“待选择规格模板”
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/engineering/hooks/use-product-mgmt.ts`
    - [x] `src/features/engineering/components/product-action-dialog.tsx`
    - [x] `src/features/engineering/services/product-type-service.ts`
    - [x] `src/features/engineering/adapters/product-type-api-adapter.ts`
    - [x] 必要时复核产品分类查询调用点与模板服务缓存链路
  - [x] 已确认本轮非目标边界：
    - [x] 不改产品模板管理页 UI
    - [x] 不顺手重构规格表单组件结构
    - [x] 不扩散到无关 BOM / Routing / 权限链路
    - [x] 不处理无关样式 warning
  - [x] 已完成本轮执行：
    - [x] 已修复产品编辑弹窗获取模板绑定信息的根因链路
    - [x] 已为模板解析失败补充最小必要的可观测性或兜底
    - [x] 已执行定向前后端校验并更新 `walkthrough.md`

- [ ] 725 已建产品编辑读取链从 `product_master_service.go` 单独抽离规划（中文），确保编辑态读取链独立、可测、低耦合且不再埋在大文件中。
  - [x] 已确认本轮目标不是继续堆叠 `product_master_service.go`，而是把“已建产品编辑读取链”抽成单独入口
  - [x] 已确认本轮关注链路是“已有产品点击编辑时的读取/模板解析补全链”，而不是整个产品主数据服务的全面重构
  - [x] 已确认当前高风险点：
    - [x] `product_master_service.go` 文件过大，编辑态读取语义埋在通用产品服务中，不利于稳定维护
    - [x] 模板派生、分类祖先链解析、编辑态读取补全属于高敏感路径，后续继续堆在大文件里容易再次出错
    - [x] 当前若不尽快把编辑读取链抽离，后续任何产品查询改动都可能误伤编辑态
  - [x] 已确认本轮建议抽离边界：
    - [x] 把“编辑态产品读取补全”收口到单独文件/函数
    - [x] 该入口负责：产品基础读取后的模板派生、分类祖先链模板绑定解析、编辑态所需补全
    - [x] 保持列表查询、批量查询、写入逻辑仍在现有 service 中，不顺手扩成整文件拆分
  - [x] 已确认本轮优先涉及文件：
    - [x] `server/services/product_master_service.go`
    - [x] 新增或拆出 `server/services` 下专门承载产品编辑读取链的文件
    - [x] `server/services/product_master_service_test.go`
    - [x] 必要时同步复核前端编辑态调用口径，但不以此轮为主
  - [x] 已确认本轮风险控制要求：
    - [x] 抽离后对外语义必须保持等价，不能改变已建产品编辑返回结构
    - [x] 必须补充回归测试覆盖直接分类模板、父分类继承模板、无模板三类场景
    - [x] 不接受只为了“文件变小”而引入多入口语义漂移
  - [x] 已确认本轮非目标边界：
    - [x] 不全面重构 `product_master_service.go` 其余 CRUD 链路
    - [x] 不顺手改产品模板管理、产品分类管理 UI
    - [x] 不扩散到 BOM / Routing / 权限 / 其它主数据模块
  - [x] 已完成本轮执行：
    - [x] 已抽离已建产品编辑读取链到独立文件/入口
    - [x] 已保持对外契约不变并补齐定向回归测试
    - [x] 已执行定向 Go 校验并更新 `walkthrough.md`

- [ ] 726 新增型号模板读取链单独抽离规划（中文），确保“新增型号选模板”与“已建产品编辑补全模板”是两条明确分离的读取路径，避免后续语义混淆。
  - [x] 已确认当前必须区分两条链路：
    - [x] 新增型号：按当前表单里的 `typeId` / 分类模板绑定实时解析模板
    - [x] 已建产品编辑：按已有产品读取补全语义解析模板，并允许历史 `templateKey` 兜底
  - [x] 已确认当前高风险点：
    - [x] 两条链若共用同一套“有效模板解析”入口，后续维护者很容易把创建态与编辑态语义混成一套
    - [x] 新增型号是“当前选择态 authority”，编辑已有产品是“既有档案补全 authority”，二者输入源不同
    - [x] 若后续误把编辑态的历史兜底逻辑带到新增态，或把新增态实时解析逻辑反向套到编辑态，都会造成灾难性误判
  - [x] 已确认本轮建议抽离边界：
    - [x] 为“新增型号模板读取链”单独抽文件/入口
    - [x] 该入口只负责创建态模板解析，不承载历史产品 `templateKey` 兜底
    - [x] 与已建产品编辑读取链保持明确命名区分，禁止混用
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/engineering/components/specs/index.ts`
    - [x] 必要时新增 `src/features/engineering` 下专门承载新增型号模板读取链的文件
  - [x] 已确认本轮风险控制要求：
    - [x] 新增态不得依赖编辑态的历史产品补全语义
    - [x] 编辑态不得回退为仅按当前表单分类实时解析
    - [x] 必须让两条路径在命名、调用点、测试上都能明显区分
  - [x] 已确认本轮建议实施范围：
    - [x] 先修正前端/后端模板绑定解析不一致或缺失的根因
    - [x] 保持产品规格编辑弹窗在模板缺失时给出明确错误态
    - [x] 不顺手重构整个产品模板系统
  - [ ] 待你确认后执行下一阶段：
    - [ ] 修复模板绑定解析链路或 fallback 逻辑
    - [ ] 验证 `山地车圈` 编辑弹窗可正确解析规格模板
    - [ ] 更新 `walkthrough.md` 并记录验证结果

- [ ] 733 产品模板绑定根因级稳住规划（中文），将模板解析 authority 从前端局部推断收口到后端一致性校验与显式解析结果。
  - [x] 已确认当前仅靠前端 fresh metadata fallback 还不够做到“任何情况都不再出问题”
  - [x] 已确认当前剩余风险点：
    - [x] 产品分类链真实缺失 `templateId`
    - [x] `templateId` 指向不存在或不可用模板
    - [x] 编辑态 `templateKey` 派生与模板真实数据不一致
    - [x] 前后端重复解析同一模板绑定语义，存在 authority 分散
  - [x] 已确认本轮根因级目标：
    - [x] 在后端增加模板绑定一致性校验
    - [x] 在产品读取链返回显式 resolved template 信息
    - [x] 前端优先消费后端 authority，减少本地重复猜测
  - [x] 已确认建议实施边界：
    - [x] 优先加固产品类型父链模板解析与产品编辑读取链
    - [x] 保留前端错误态展示，但不再让前端成为模板绑定 authority
    - [x] 不扩成整套模板系统重构
  - [x] 已确认优先涉及文件：
    - [x] `server/services/product_edit_read_service.go`
    - [x] `server/services/product_master_service.go`
    - [x] 产品类型 / 产品模板相关 handler、service、DTO、mapper
    - [x] `src/features/engineering/components/product-action-dialog.tsx`
    - [x] 必要时联动前端 product contract / adapter
  - [x] 已确认验证目标：
    - [x] 编辑态与创建态的模板解析结果一致
    - [x] 后端对无效分类模板绑定给出明确错误
    - [x] 前端不再因为本地旧上下文与后端 authority 漂移而误判
  - [ ] 待你确认后执行下一阶段：
    - [ ] 实施后端模板绑定一致性校验
    - [ ] 实施产品读取链 resolved template authority 收口
    - [ ] 前端切换为优先消费后端解析结果
    - [ ] 执行定向 TS / Go 校验并更新 `walkthrough.md`

- [ ] 734 新建型号模板链路加固规划（中文），让创建态模板解析 authority 与编辑态保持一致，避免创建态继续只靠前端父链推断。
  - [x] 已确认当前编辑态 authority 已收口到后端读取链，但创建态仍主要依赖前端按 `typeId -> parent chain -> template` 推断
  - [x] 已确认当前创建态仍存在的风险点：
    - [x] 产品分类父链在前端 options 上下文中不完整
    - [x] `templateId` 指向不存在或不可用模板
    - [x] 创建态与编辑态对同一分类给出不同模板结果
    - [x] 前端缓存 / 上下文漂移再次导致模板误判
  - [x] 已确认本轮根因级目标：
    - [x] 为创建态提供后端 authoritative template resolution
    - [x] 让创建态与编辑态消费同一套模板裁决语义
    - [x] 保留前端错误态展示，但不再让创建态承担主 authority
  - [x] 已确认建议实施方向：
    - [x] 优先新增或扩展后端创建态模板解析入口（按 `typeId` 返回 resolved template result）
    - [x] 或在产品类型 options 返回中直接附带该分类的有效模板解析结果
    - [x] 前端创建态优先消费后端解析结果，仅保留最小本地兜底
  - [x] 已确认优先涉及文件：
    - [x] 产品类型 / 模板相关后端 service、handler、route、DTO
    - [x] `src/features/engineering/components/product-action-dialog.tsx`
    - [x] `src/features/engineering/utils/product-create-template-resolution.ts`
    - [x] 必要时联动 `ProductTypeService` / 前端 contract / adapter
  - [x] 已确认验证目标：
    - [x] 创建态选择分类时能稳定解析模板
    - [x] 创建态与编辑态对同一分类返回一致模板结果
    - [x] 模板真实缺失时可给出明确错误而非误判
  - [ ] 待你确认后执行下一阶段：
    - [ ] 实施创建态后端模板解析 authority
    - [ ] 前端创建态切换为优先消费后端结果
    - [ ] 执行定向 TS / Go 校验并更新 `walkthrough.md`
- [ ] 731 `use-bom-form.ts` 最小职责拆分规划（中文），确保 BOM 表单初始化、选项读取、状态修正与 delta 跟踪不再继续堆叠在同一个 form hook 中。
  - [x] 已确认当前最值得作为下一阶段拆分点的是 `src/features/engineering/hooks/use-bom-form.ts`
  - [x] 已确认当前职责混杂点：
    - [x] 表单初始化与选项读取混杂在 `useBOMForm` 中
    - [x] 表单状态修正与 delta 跟踪混杂在 `useBOMForm` 中
    - [x] `useBOMForm` 内部存在多个不相关的副作用
  - [x] 已确认当前风险点：
    - [x] 若继续堆叠职责，后续维护将变得困难
    - [x] 若不及时拆分职责，将导致 BOM 表单逻辑变得难以理解
    - [x] 若不及时拆分职责，将导致 BOM 表单 bug 难以定位
    - [x] 当前模块的数据层与展示层都未统一接入 `useLanguage / t(...)`
  - [x] 已确认本轮建议实施边界：
    - [x] quick-action registry 不再存最终展示文案，改存 i18n key
    - [x] drawer / handle 接入 `useLanguage()` 与 `t(...)`
    - [x] 补齐 `zh-CN / en-US` 对应 locale 文案
    - [x] 保持权限筛选与动作路由逻辑不变，只修国际化文案链路
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/quick-actions/data/quick-action-registry.ts`
    - [x] `src/features/quick-actions/components/quick-action-drawer.tsx`
    - [x] `src/features/quick-actions/components/quick-action-handle.tsx`
    - [x] `src/features/quick-actions/types.ts`
    - [x] `src/locales/messages/zh-CN/*`
    - [x] `src/locales/messages/en-US/*`
  - [x] 已确认本轮非目标边界：
    - [x] 不改 quick-actions 权限判定逻辑
    - [x] 不顺手扩展新的快捷动作入口
    - [x] 不改扫描页面业务逻辑
  - [x] 已完成本轮执行：
    - [x] 已重构 quick-actions registry 的文案承载方式
    - [x] 已接入 `useLanguage / t(...)` 并补齐中英文 locale
    - [x] 已执行定向 TypeScript 校验并更新 `walkthrough.md`

- [ ] 728 `/engineering/bom` 页面 500 根因排查与修复规划（中文），确保 BOM 页面读取链不再因为列表接口异常而被前端 fail loudly 放大成整页崩溃。
  - [x] 已确认当前报错现象：
    - [x] `/engineering/bom` 页面加载时出现 500
    - [x] 前端控制台在 `useBOMData.boms` 抛出 `[CRITICAL] BOM data is missing after load`
    - [x] `BOMMgmt` 被 React error boundary 接管并重建
  - [x] 已确认当前真实故障链路：
    - [x] `BOMMgmt` 调用 `useBOMData()`
    - [x] `useBOMData` 通过 React Query 调用 `bomService.getBOMs()`
    - [x] `bomService.getBOMs()` 请求 `/engineering/bom`
    - [x] 当接口返回失败时，`bomsQuery.data` 在加载结束后仍为空
    - [x] `useBOMData` 当前对“加载完成但无数据”直接 `failLoudly + throw`，因此把上游失败放大成页面级崩溃
  - [x] 已确认当前高风险点：
    - [x] 前端把“数据缺失”与“接口失败”混成一个 `[CRITICAL]` 分支，缺少清晰区分
    - [x] `/engineering/bom` 列表接口一旦 500，当前页面不是显示错误态，而是直接崩
    - [x] 后端 `ListBOMs` 预加载 `Product / ChangeOrder / Items / Substitutes / Material`，任何一层契约或查询异常都可能导致整个列表失败
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/engineering/hooks/use-bom-data.ts`
    - [x] `src/features/engineering/services/bom-service.ts`
    - [x] `src/features/engineering/tabs/bom-mgmt.tsx`
    - [x] `server/handlers/bom.go`
- [ ] 722 BOM 剩余枚举/日期控制字段统一收口规划（中文），记录 changeType/status/effectiveFrom/effectiveTo 的排查结论、推荐实施范围与待确认执行项。
  - [x] 已确认本轮最明确的字段包括：
    - [x] `changeType`
    - [x] `status`
    - [x] `effectiveFrom`
    - [x] `effectiveTo`
  - [x] 已确认当前并非完全无规则，而是 authority 分散：
    - [x] `src/lib/codecs/code-normalization.ts` 已提供 `normalizeBomChangeType / normalizeBomStatus / normalizeBomEffectiveDate`
    - [x] `src/features/engineering/components/bom-mgmt/bom-table.tsx` 已在展示侧使用上述 helper
    - [x] `src/features/engineering/components/bom-mgmt/bom-preview.tsx` 已在展示侧使用上述 helper
    - [x] `src/features/engineering/data/schema.ts` 已对 `changeType / status / effectiveFrom / effectiveTo` 定义约束或默认值
  - [x] 已确认当前真实问题不是“字段缺少规则”，而是“BOM 侧仍未把这组字段收口到单一 authority”：
    - [x] `src/features/engineering/components/bom-editor/bom-form-header.tsx` 仍在组件内分散处理 `changeType / status / effectiveFrom / effectiveTo`
    - [x] `src/features/engineering/utils/product-code-normalization.ts` 虽然提供了 `normalizeBOMInput(...)`，但主要发生在保存前
    - [x] 创建态默认值、编辑态回填、输入时 normalize、保存前 sanitize、展示层 normalize 目前仍分散在多个层级
  - [x] 已确认当前主要风险点：
    - [x] 若继续在 `bom-form-header.tsx` 内局部补丁，后续很难保证初始化/输入/保存三层语义一致
    - [x] 若只依赖保存前 `normalizeBOMInput(...)`，UI 层仍可能长期保留不规范中间态
    - [x] 若未来继续扩充 BOM 表单字段，分散 authority 会继续放大维护成本
  - [x] 已确认本轮建议实施范围：
    - [x] 优先收口 BOM 表单输入链
    - [x] 保留保存前 `normalizeBOMInput(...)` 作为最终防线
    - [x] 保留 table / preview 展示层 normalize 作为展示兜底
    - [x] 尽量把组件层分散字段处理下沉到更稳定的表单 authority
  - [x] 已确认本轮优先涉及文件：
    - [x] `src/features/engineering/components/bom-editor/bom-form-header.tsx`
    - [x] `src/features/engineering/hooks/use-bom-form.ts`
    - [x] `src/features/engineering/hooks/use-bom-form-initialization.ts`
    - [x] `src/features/engineering/tabs/bom-mgmt.tsx`
    - [x] `src/features/engineering/services/bom-service.ts`
  - [x] 已确认本轮非目标边界：
    - [x] 不扩到 `items / substitutes`
    - [x] 不改 BOM 表格/预览的 UI 结构
    - [x] 不处理无关样式 warning

- [ ] 735 应收应付记录级证据挂载规划（中文），让每一笔收款/付款记录都能独立挂载图片证据，并在详情中按记录联动展示。
  - [x] 已确认本轮目标不是给台账或订单底部临时加附件区，而是为 `ReceiptRecord / PaymentRecord` 增加独立证据挂载能力
  - [x] 已确认当前已有基础能力：
    - [x] 应收/应付详情已具备 `receiptRecords / paymentRecords`
    - [x] 当前系统已具备图片上传基础设施与订单级 evidence 组件
    - [x] 当前系统已具备核销分摊能力，可表达部分收款/付款
  - [x] 已确认当前核心缺口：
    - [x] 收款记录与付款记录当前没有独立 evidence 绑定关系
    - [x] 订单级 `evidences` 不能替代记录级财务证据
    - [x] 详情页当前只能看记录和分摊，不能按记录联动展示截图
  - [x] 已确认本轮最小实施范围：
    - [x] 先支持收款记录级图片证据挂载
    - [x] 先支持付款记录级图片证据挂载
    - [x] 前端详情页支持“选中记录 -> 展示该记录证据”
    - [x] 暂不扩到 PDF / Excel / OCR / 阶段款语义
  - [x] 已确认本轮建议的解耦原则：
    - [x] 资源上传层与业务挂载层分离
    - [x] 不直接把 evidences JSON 塞进 `receipt_records / payment_records`
    - [x] 不复用订单 evidence 语义承载财务记录证据
    - [x] 详情页长组件优先拆出独立 evidence section，避免继续堆叠
  - [x] 已确认本轮优先涉及文件：
    - [x] `server/models` 下新增 settlement evidence 模型文件
    - [x] `server/services` 下新增 settlement evidence service / dto / mapper
    - [x] `server/handlers` 下新增 settlement evidence handler
    - [x] `server/routes` 下新增 settlement evidence route
    - [x] `src/features/trading/settlement-evidences` 下新增 contracts / services / hooks / components
    - [x] `src/features/trading/receivables/components/sales-receivable-detail-dialog.tsx`
    - [x] `src/features/trading/payables/components/purchase-payable-detail-dialog.tsx`
  - [x] 已确认本轮主要风险点：
    - [x] 若混用订单级 evidence 与记录级 evidence，会导致财务证据语义混乱
    - [x] 若把上传、挂载、展示逻辑继续堆进现有 500+ 行 dialog，维护成本会继续恶化
    - [x] 若一开始就扩到文档类附件与阶段款语义，任务范围会失控
  - [ ] 待执行：
    - [ ] 新增后端记录级 evidence 模型、服务、handler、route 骨架
    - [ ] 新增前端 settlement evidence contract / service / hooks / components
    - [ ] 改造应收详情，支持按收款记录查看与上传图片证据
    - [ ] 改造应付详情，支持按付款记录查看与上传图片证据
    - [ ] 执行定向 Go / TS 校验并更新 `walkthrough.md`
  - [ ] 视觉收口新增目标（待确认后执行）：让记录级证据区与系统现有 trading 详情视觉语言完全对齐
    - [x] 已确认这一步是新增范围，不再是上一轮 MVP 的默认延伸
    - [x] 已确认本轮目标不是新增业务能力，而是只做视觉收口
    - [x] 已确认本轮重点检查项：
      - [x] 卡片标题字体 / 字号 / 字重 / 是否斜体
      - [x] 空态容器与文案层级
      - [x] badge 风格与颜色语义
      - [x] 上传区与 gallery 卡片的边框、圆角、间距、按钮层级
      - [x] 应收 / 应付详情中新增区块与既有区块的节奏一致性
    - [x] 已确认本轮优先涉及文件：
      - [x] `src/features/trading/settlement-evidences/components/settlement-record-evidence-panel.tsx`
      - [x] `src/features/trading/settlement-evidences/components/settlement-evidence-upload.tsx`
      - [x] `src/features/trading/settlement-evidences/components/settlement-evidence-gallery.tsx`
      - [x] `src/features/trading/receivables/components/sales-receivable-detail-dialog.tsx`
      - [x] `src/features/trading/payables/components/purchase-payable-detail-dialog.tsx`
    - [x] 已确认本轮非目标边界：
      - [x] 不新增后端接口
      - [x] 不改记录级 evidence 业务语义
      - [x] 不顺手重构整个应收应付详情结构
    - [ ] 待执行：
      - [ ] 对齐标题、空态、badge、上传区与 gallery 的系统视觉风格
      - [ ] 对齐新增筛选按钮与既有 dialog 控件层级
      - [ ] 执行前端定向校验并更新 `walkthrough.md`

- [ ] 736 客户卡片微信打开入口最小实现规划（中文），仅支持从客户卡片打开微信，不扩展审计、Webhook、时间轴或企微侧边栏集成。
   - [x] 已确认本轮范围严格收敛为“打开微信”最小能力：
    - [x] 本轮实际业务能力只落地微信入口
    - [x] 但代码组织需按多渠道扩展预留独立目录/文件
    - [x] 不做沟通审计
    - [x] 不做消息回传
    - [x] 不做企微侧边栏或会话存档
  - [x] 已确认当前前置缺口：
    - [x] `src/features/trading/data/schema.ts` 的 `Customer` 结构尚无微信字段
    - [x] `src/features/trading/customer/contracts/customer-api-dto.ts` 尚无微信字段
    - [x] `src/features/trading/components/customer-action-dialog.tsx` 尚无微信输入项
    - [x] `src/features/trading/components/customer-list.tsx` 尚无微信打开按钮
  - [x] 已确认本轮最小实施范围：
    - [x] 新增独立 communications/contact-channels 目录承载渠道能力
    - [x] 为客户资料补充渠道字段或最小微信字段支撑
    - [x] 在客户编辑弹窗提供微信录入
    - [x] 在客户卡片增加渠道入口按钮，但当前只启用微信
    - [x] 将 deep link / scheme 拼装逻辑放入独立文件，避免散落在客户卡片组件
  - [x] 已确认本轮非目标边界：
    - [x] 不保证直接命中某个客户对话框
    - [x] 首轮跳转能力仍只启用微信，其他渠道先只记录资料
    - [x] 不处理个人微信与企业微信复杂兼容策略
    - [x] 不新增后端沟通事件模型
    - [x] 不改客户时间轴或订单拓扑
    - [x] 不处理无关 CRM / trading 视觉重构
  - [ ] 范围已变更，需重新确认“多渠道资料记录”方案：
    - [ ] 在客户资料中统一补充渠道字段：`wechat / whatsapp / facebook / instagram / telegram`
    - [ ] 客户编辑弹窗新增上述渠道输入项
    - [ ] 当前仅 `wechat` 保留已实现的卡片打开入口
    - [ ] `whatsapp / facebook / instagram / telegram` 本轮仅保存与展示，不做跳转按钮
    - [ ] 前后端客户契约与持久化链路统一补齐新增字段
    - [ ] 执行定向 TypeScript / Go 编译型校验并更新 `walkthrough.md`

- [ ] 736-扩展：将客户沟通渠道/卡片交互方案复用到供应商（中文）
  - [ ] 待确认本轮扩展范围：
    - [ ] 为供应商资料补充与客户一致的沟通渠道字段：`wechat / whatsapp / facebook / instagram / telegram / email`
    - [ ] 在供应商编辑弹窗中新增“沟通渠道”分组
    - [ ] 供应商卡片不再支持点击卡片本体直接编辑，只保留菜单编辑入口
    - [ ] 若供应商卡片已有微信入口，则保持仅微信可跳转；若尚无，则本轮可按客户同口径补最小微信入口
    - [ ] 其余渠道本轮仅保存与展示，不做跳转按钮
    - [ ] 前后端供应商契约、保存链路、历史数据兼容策略按客户同标准补齐
    - [ ] 执行定向 TypeScript / Go 编译型校验并更新 `walkthrough.md`

- [ ] 736-扩展：销售侧客户信息增强（中文）
  - [ ] 待确认本轮增强范围：
    - [ ] 客户卡片从当前双列半宽改为单列占满，给业务摘要留出横向空间
    - [ ] 客户卡片直接展示真实联动闭环摘要，而不是接粗粒度 analytics 聚合
    - [ ] 优先展示：是否存在未完成订单、最后下单时间
    - [ ] 可选展示：历史订单数、累计销售额，但仅在来源于真实订单链或可严格联动时才接入
    - [ ] 当前明确不动现有 analytics 粗聚合口径，除非后续做到真正业务联动
    - [ ] 未完成订单判断应基于真实销售订单状态：`Draft / Pending / InProgress` 视为未完成，`Done / Canceled` 视为已闭环
    - [ ] 最后下单时间应直接来自销售订单主单时间链，而不是前端重算粗聚合结果
    - [ ] UI 仅做客户卡片信息增强，不顺带改动销售订单主流程
    - [ ] 执行定向 TypeScript / Go 编译型校验并更新 `walkthrough.md`

- [ ] 736-扩展：客户卡片闭环入口改造（中文）
  - [ ] 待确认本轮改造范围：
    - [ ] 明确区分“时间线记录入口”和“完整闭环入口”两种职责
    - [ ] 时间线入口仅负责记录，不承担完整业务处理闭环
    - [ ] 客户卡片新增主按钮“查看完整订单”或同义文案，作为主闭环入口
    - [ ] 主闭环入口复用现有销售订单管理页，不新造客户业务中心或重复订单面板
    - [ ] 销售订单页新增 `customerId`（可选 `customerName`）上下文承接能力
    - [ ] 订单管理页进入后应按客户上下文落在对应订单视图，而不是仅依赖模糊 `search`
    - [ ] 审计记录入口降级为次级入口，避免与业务闭环主入口混淆
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：客户卡片订单闭环显示口径调整（中文）
  - [ ] 待确认本轮改造范围：
    - [ ] 订单闭环主值改为“已完成数 / 总订单数”而不是仅显示“有未完成订单”
    - [ ] 无订单客户固定显示 `0/0`，并配套“暂无订单”语义
    - [ ] 有未完成订单时支持直观看到如 `0/1`、`2/5` 这类闭环完成度
    - [ ] 复用现有 `openOrderCount / totalOrders` 数据，不新增后端接口
    - [ ] 前端仅做稳定派生：`closedOrderCount = totalOrders - openOrderCount`，并做好非负保护
    - [ ] 保留最后下单时间与沉默时长，但不抢占闭环主值视觉层级
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：个人记录缓冲区模块（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 新建独立文件夹与文件承载个人记录缓冲区模块，不挂靠现有客户/订单/审计模块
    - [ ] 功能定位为账户私有的个人临时记录缓冲区，仅本人可见
    - [ ] 管理员无审计权，不提供后台查看入口
    - [ ] 图片复用现有图片压缩链，统一输出 WEBP，不重复造上传/压缩逻辑
    - [ ] 不做备份能力，账户删除时级联删除其个人记录与相关图片引用
    - [ ] 当前阶段只做个人记录/整理/归档，不实现正式业务对象流转
    - [ ] 但数据模型与模块边界需预留未来从个人记录发起业务调用的 action 扩展位
    - [ ] 前端建议采用弹窗 + 独立页面双入口，Trello 式轻量列结构优先
    - [ ] 执行定向 TypeScript / Go 编译型校验并补充 `walkthrough.md`

- [ ] 736-扩展：个人记录缓冲区拖拽排序（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在个人记录缓冲区四列看板中支持列内排序与跨列拖拽
    - [ ] 拖拽后同步更新记录的 `columnKey` 与 `sortOrder`，保证刷新后顺序稳定
    - [ ] 尽量复用现有看板与卡片结构，不重写整套弹窗/页面 UI
    - [ ] 后端仅补最小批量排序更新接口，不扩展到删除、恢复、共享等无关能力
    - [ ] 前端优先保证桌面端拖拽交互清晰，移动端先保持可浏览，不强行做复杂手势排序
    - [ ] 为后续多图、action 扩展保留兼容性，不把排序逻辑耦合进图片或业务动作模型
    - [ ] 执行定向 TypeScript / Go 编译型校验并补充 `walkthrough.md`

- [ ] 736-扩展：个人记录弹窗完整相机面板（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在个人记录新建/编辑弹窗中提供页面内相机预览，而不仅是系统文件选择
    - [ ] 支持拍照按钮，将当前视频画面截帧为图片并走现有上传链
    - [ ] 支持切换前置/后置摄像头，优先适配手机端录入场景
    - [ ] 在浏览器不支持 `getUserMedia`、权限被拒绝或非安全上下文时，自动回退到当前文件选择/系统拍照方案
    - [ ] 不引入第二套上传能力，拍照产物仍统一接入现有图片上传接口
    - [ ] 不在本轮扩展为录像、连续拍摄、照片库管理或多图编辑
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：个人记录完整页模块壳布局（方案B，中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将个人记录完整页从当前“PageHeader + 看板直出”改为带正式模块壳的页面结构
    - [ ] 顶部增加模块级承载层，可采用与现有模块一致的 tab/操作条风格，但不强行扩展多业务 tab
    - [ ] 看板区域退回正文层，不再重复承担页面标题职责
    - [ ] 完整页优先对齐系统内正式模块页的视觉骨架与留白节奏
    - [ ] 弹窗入口保持轻量，不要求与完整页完全同构
    - [ ] 不在本轮顺带扩展共享、报表、筛选器中心等二次复杂能力
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：个人记录媒体面板视频录制（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在现有相机面板中增加单段视频录制能力
    - [ ] 单次录制时长限制为最多 10 秒，并提供录制中剩余秒数提示
    - [ ] 达到 10 秒后自动停止录制，也允许用户手动提前停止
    - [ ] 录制完成后直接走现有资源上传链，不新增第二套上传协议
    - [ ] 继续保留拍照模式，与视频录制模式并存但不混淆
    - [ ] 在浏览器不支持 `MediaRecorder`、权限失败或设备能力不足时，自动降级到拍照/文件上传
    - [ ] 不在本轮扩展视频裁剪、封面编辑、多段拼接、长视频录制
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-阶段收口：个人记录视频录制反馈强化（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在视频录制进行中为媒体面板增加红色高亮边缘与呼吸灯效果
    - [ ] 强化录制中状态提示，使用户能明确感知当前正在记录现场
    - [ ] 保持现有 10 秒限制、自动停止、手动停止与上传链不变
    - [ ] 不在本轮修改码率、分辨率、离线缓存或转发协议
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-重规划：个人记录媒体能力改为本地优先（中文）
  - [ ] 待确认后续方向：
    - [ ] 拍照与视频录制结果优先保存在本地，而不是默认立即上传到服务器
    - [ ] 媒体面板中新增显式“上传到服务器”入口，由用户主动决定是否上云
    - [ ] 未上传的本地媒体允许手动丢弃，避免无价值现场记录占用云端资源
    - [ ] 设计上优先支持“手机采集、电脑处理”的协同场景，而不是默认所有采集结果都进入云端
    - [ ] 本地暂存应考虑 IndexedDB 或等效浏览器本地持久化方案，不依赖 Redis
    - [ ] 不在本轮直接扩展为分片上传、视频增强、转发协议升级
    - [ ] 后续实施前需先更新技术方案并等待确认

- [ ] 736-第一小步：个人记录本地草稿模型与 IndexedDB 暂存（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为拍照与视频录制结果建立本地草稿媒体模型，区分“未上传草稿”与“已上传媒体”
    - [ ] 通过 IndexedDB 保存本地草稿，使页面刷新后仍可恢复
    - [ ] 本轮只落本地暂存基础层，不实现“上传到服务器”按钮
    - [ ] 本轮不改服务器接口，不改转发协议，不做多端同步
    - [ ] 本轮需要为后续手动上传与本地丢弃预留字段和状态
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-下一步：个人记录媒体手动上传入口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 停止拍照、视频录制与文件选择后的默认即时上传行为
    - [ ] 媒体面板中新增显式“上传到服务器”按钮，由用户主动触发上传
    - [ ] 仅允许对本地草稿媒体执行上传，不改变当前服务器接口
    - [ ] 上传成功后将本地草稿状态切换为已上传，或将其映射到云端引用
    - [ ] 保持本地草稿恢复能力不变，为后续“丢弃草稿”功能预留位置
    - [ ] 本轮不实现本地草稿删除 UI，不做多端同步，不做协议升级
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-下一步：个人记录本地草稿删除/丢弃入口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为当前活跃本地草稿增加显式“丢弃”入口
    - [ ] 丢弃后同步清理 IndexedDB 中对应草稿记录
    - [ ] 若当前草稿尚未上传，则直接清空当前草稿态与预览
    - [ ] 若当前草稿已上传，本轮只删除本地草稿记录，不删除云端资源
    - [ ] 若当前表单仍引用被丢弃草稿，需要明确是否同步清空当前媒体值
    - [ ] 本轮不做草稿列表中心，不做批量删除，不做云端资源删除
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：快捷扫描右侧边栏接入个人缓冲区快捷媒体入口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在快捷扫描右侧边栏中新增“个人拍照”“个人录视频”“个人缓冲区”快捷入口
    - [ ] 这些入口归属于当前登录账号的个人缓冲区，不混入仓库扫描动作语义
    - [ ] 通过独立 capture 页面承接一键拍照/一键录视频模式，而不是直接复用编辑弹窗
    - [ ] 支持将快捷入口添加到手机主屏幕，作为桌面快捷方式
    - [ ] 不承诺浏览器直接将媒体文件写入手机桌面文件系统，避免超出 Web 能力边界
    - [ ] 本轮不扩展为草稿中心、多端同步或云端协议升级
    - [ ] 后续实施前需先更新技术方案并等待确认

- [ ] 736-方案A：快捷采集成功后自动打开新建个人记录弹窗（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 快捷拍照或快捷录视频成功后，自动打开“新建个人记录”弹窗
    - [ ] 将刚采集生成的本地草稿媒体自动回填到弹窗中
    - [ ] 弹窗继续承担标题、备注、分栏等记录整理职责
    - [ ] 需要在 capture 页与 `PersonalWorkbenchCardEditor` 之间增加桥接状态
    - [ ] 本轮不做草稿中心改造，不做多端同步，不改后端协议
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-扩展：快捷扫描个人卡片增加“放到桌面”入口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在快捷扫描侧边栏的“个人拍照 / 个人录视频 / 个人缓冲区”卡片上增加“放到桌面”入口
    - [ ] 优先使用 PWA 的安装能力或浏览器原生安装提示
    - [ ] 当浏览器不支持直接安装时，降级为“添加到主屏幕”文案指引
    - [ ] 安装后的入口需能区分个人拍照、个人录视频、个人缓冲区三类快捷目标
    - [ ] 不承诺直接创建原生系统级快捷方式，不做浏览器外桌面文件写入
    - [ ] 实施前需先更新技术方案并等待确认

- [ ] 736-修复：个人记录缓冲区 404 / 500 / Dialog 警告（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将个人缓冲区接口 404 归类为环境 / 代理 / 后端版本同步核对项，不误判为前端 service 路径错误
    - [ ] 为个人缓冲区完整页与弹窗补充 query 错误兜底，避免接口异常放大为整页 500
    - [ ] 调整个人缓冲区弹窗与编辑弹窗结构，避免嵌套 `Dialog` 带来的可访问性警告
    - [ ] 不在本轮改个人记录后端协议，不重写个人记录模块整体交互
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 736-后续：个人缓冲区快捷采集链路分阶段加固（中文）
  - [ ] 待确认第一阶段（必修）范围：
    - [ ] 为本地媒体草稿补充账号隔离字段，避免同浏览器多账号串草稿
    - [ ] 读取本地草稿时按当前账号过滤
    - [ ] 不在本阶段改后端协议，仅修正前端本地草稿隔离
  - [ ] 待确认第二阶段（次优先级）范围：
    - [ ] 收敛草稿预览、上传结果、表单媒体引用之间的状态源，减少 `value + activeDraft + previewUrl` 多头驱动
    - [ ] 明确快捷采集保存成功后的去向，至少确定成功后是留在当前采集页、返回个人缓冲区还是进入下一条整理动作
    - [ ] 将自动录视频从“进入即自动开始录制”优化为更稳的“自动进入录制准备态 + 用户确认开始”或等价交互
    - [ ] 尽量不改第一阶段已完成的账号隔离数据层，仅收敛媒体面板与 capture 页编排
  - [ ] 待确认第三阶段（增强）范围：
    - [ ] 增加待整理草稿队列，让快捷采集后的多条草稿可以顺序整理，而不是一次只处理一条
    - [ ] 评估并接入 manifest shortcuts，作为放到桌面之外更标准的 PWA 快捷入口补充
    - [ ] 将本地媒体草稿升级为更明确的状态机，至少区分本地草稿、上传中、已上传、已关联记录等状态
    - [ ] 不在第三阶段回退前两阶段已确认的账号隔离与录制准备态策略

- [ ] 736-深层增强：个人缓冲区历史迁移、队列批量能力与 shortcuts 兼容（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为历史旧草稿增加状态批量迁移策略，将旧 `draft / uploaded` 兼容映射到新状态机
    - [ ] 为待整理草稿队列增加优先级与批量操作能力，例如置顶、跳过、批量清理已关联项
    - [ ] 为 shortcuts 增加跨浏览器兼容增强，避免仅依赖单一 manifest/安装路径
    - [ ] 不回退前三阶段已完成的账号隔离、录制准备态、队列整理与保存后流转
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 737-个人工作收纳箱第一批 MVP：便签与链接（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 新增“便签”类型，支持直接粘贴文本并以较大容器样式展示
    - [ ] 新增“链接”类型，支持通过 `+` 号新增链接，并保存 URL 与备注
    - [ ] 第一批目标是承接杂乱边缘信息，不强行挂入正式业务模块
    - [ ] 第一批不做联系人、提醒、复杂标签系统与业务对象关联
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 738-问题修复：个人工作台权限映射、service worker warning 与 notifications websocket 排查（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 修复 `/personal-workbench` 顶层路径未映射到 permission catalog 导致的权限生成报错
    - [ ] 清理 `public/sw.js` 中 no-op fetch handler，消除浏览器导航 warning
    - [ ] 排查 `useNotifications` websocket error 是前端日志/重连策略问题，还是后端/代理不可用问题
    - [ ] 若 websocket 根因在后端或部署层，本轮先补前端降噪与诊断，不臆造前端补丁
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 739-personal-workbench 顶部统一 Tab 结构（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在 `/personal-workbench` 顶部引入统一 Tab，至少包含“个人记录缓冲区”和“工作收纳箱”
    - [ ] 保持工作收纳箱与个人媒体缓冲区底层能力隔离，仅统一入口与切换体验
    - [ ] 尽量复用现有 `tabs` 组件，不额外引入复杂导航结构
    - [ ] 不回退当前已完成的工作收纳箱独立路由与本地存储隔离
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 740-组织人事导航重构：新增独立侧边栏分组并整体挂载 /personnel（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 新增一个独立侧边栏分组：`组织人事`
    - [ ] 第一轮先把当前 `/personnel` 入口整体挂入该分组
    - [ ] 不细拆组织人员 / 账号权限等内部子分组
    - [ ] `KPI绩效` 暂不接入，避免扩大范围与引起额外反感
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 741-/personnel/leave 与 /personnel/stats 本地化收口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 清理 `/personnel/leave` 页面中残留的中英混排硬编码文案
    - [ ] 清理 `/personnel/stats` 页面中残留的中英混排硬编码文案
    - [ ] 补齐 `orgPersonnel` 相关中英文语言包，页面统一改走 `t(...)`
    - [ ] 评估并收口 `leave-display.ts` 中固定中文标签与固定 `zh-CN` 时间格式
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 742-新增“服务中心”同级侧边栏分组（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 新增与 `组织人事` 同级的侧边栏分组：`服务中心`
    - [ ] 将 `请假管理` 与 `荣誉榜` 从 `组织人事` 中迁出并挂入该分组
    - [ ] 保持现有页面路由与业务逻辑不变，仅调整导航归属
    - [ ] 暂不继续细拆更多二级菜单
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 743-个人工作台采用页内搜索方案（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 个人工作台内仅保留一个统一搜索入口，同时搜索 `个人记录缓冲区` 与 `工作收纳箱`
    - [ ] 搜索结果按 Tab 分开展示，兼顾移动端查看与切换
    - [ ] 明确不接入全局搜索，不出现在全局搜索结果中
    - [ ] 搜索范围仅限当前登录人的个人内容，保持“仅自己可见”心智
    - [ ] 在页面文案中明确“统一搜索我的内容 / 仅搜索你自己的内容”边界
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 744-销售管理侧边栏一级分类收口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 新增侧边栏一级分类：`销售管理`
    - [ ] 先将当前已有 `/trading` 销售管理入口整体迁入该分类
    - [ ] 本轮只做分类收口，不提前细拆 `客户管理 / 报价管理 / 销售订单`
    - [ ] 保持现有销售页面路由与业务逻辑不变，仅调整导航归属
    - [ ] 命令搜索与中英文导航文案同步调整归属
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 745-销售管理内部TAB重排与报价管理占位页（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将 `trading` 内部 TAB 顺序调整为 `客户管理 / 报价管理 / 销售订单 / 物流管理 / 应收 / 订单分析`
    - [ ] 新增 `报价管理` TAB、路由与占位页
    - [ ] 本轮仅提供报价管理占位页，不接完整报价业务能力
    - [ ] 保持现有 `客户 / 订单 / 物流 / 应收 / 分析` 页面逻辑不变
    - [ ] 如有必要同步更新命令搜索与中英文 trading 文案
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 746-报价管理改为侧边栏独立同级菜单（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] `报价管理` 不再作为 `trading` 内部 TAB，而是改为侧边栏中与 `销售管理` 同级的独立菜单项
    - [ ] `销售管理` 内部恢复聚焦 `客户管理 / 销售订单 / 物流管理 / 应收 / 订单分析`
    - [ ] 当前 `/trading/quotes` 占位实现需评估为迁移或回退，不保留为最终挂载位置
    - [ ] 新的 `报价管理` 页面应预留自己的内部多 TAB 结构，以承载零售模板、批发模板等后续拆分
    - [ ] 同步调整侧边栏、命令搜索与中英文文案归属
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 747-销售单据共用模板层抽取（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 先从正式销售订单中提炼可供 `销售订单 / 报价管理` 共同复用的单据模板层
    - [ ] 优先抽取 `表头模板 / 明细编辑模板 / 底部统计或附件模板`
    - [ ] 共用的是 UI 模板层与字段骨架，不在本轮强行合并 API、保存逻辑、状态流
    - [ ] 报价管理后续接入时优先复用该模板层，避免两边表单分叉维护
    - [ ] 新增模板层文件优先独立目录化，避免继续堆叠在销售订单专用文件中
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 748-报价管理单一主TAB方案（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] `报价管理` 先只保留一个主 TAB：`报价单`
    - [ ] 客户作为筛选维度，不在报价管理中新增一级 `客户` TAB
    - [ ] `零售 / 批发` 先作为数据类型字段或筛选项，不上升为一级 TAB
    - [ ] 报价管理首页先围绕 `报价单列表 + 客户筛选 + 状态筛选 + 历史版本/作废入口 + 转正式单入口` 组织
    - [ ] 后续如真实业务需要，再评估扩展 `模板规则 / 加成计算 / 历史作废` 等独立 TAB
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 749-报价单工作区真实骨架（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 先在 `报价单` 主工作区落地真实页面骨架，而不是继续调整导航结构
    - [ ] 首先补齐 `客户 / 状态 / 类型` 筛选栏与 `新建报价` 主入口
    - [ ] 列表区先实现 `报价单列表骨架 + 空态 + 占位操作栏`，不在本轮直接接入完整业务数据流
    - [ ] 预留 `历史版本 / 作废记录 / 转正式销售订单` 动作入口，但先只做结构占位
    - [ ] 录入/编辑区域优先复用已抽出的 `sales-document` 共用模板层，不重新复制销售订单表单
    - [ ] 本轮不提前合并报价 API、保存逻辑、审批流与正式销售订单状态流
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 750-报价单列表假数据与服务层接口骨架（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为 `报价单` 工作区补齐最小可运行的数据层：先使用假数据或本地 mock service，而不是直接接入完整后端
    - [ ] 让现有 `客户 / 状态 / 类型 / 关键词` 筛选区与列表结果真正联动
    - [ ] 报价列表先沉淀 `quote summary` 级别字段骨架：编号、客户、类型、状态、更新时间、金额摘要等
    - [ ] 服务层优先独立成 `data / service / hook` 形态，避免后续真实接口接入时再次重构页面层
    - [ ] 保留未来切换为真实 API 的适配边界，不在本轮直接接入完整 CRUD、审批、转单事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 751-报价列表真实接口适配层（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在现有 `mock service` 基础上补齐真实接口适配层，但本轮只优先接 `报价列表只读接口`
    - [ ] 页面层继续只消费 `hook / service`，不直接请求 API，保持 `mock -> real api` 可切换
    - [ ] 先收口列表接口返回的 `quote summary` 映射，不在本轮直接拉通详情、编辑、保存与转单事务
    - [ ] 若后端接口未就绪，则保留本地 mock 回退能力，避免页面失稳
    - [ ] 本轮不接报价详情、不接完整 CRUD、不接审批流与转正式销售订单事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 752-固定正式报价列表接口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 固定后端正式报价列表接口为单一路径，不再继续保留多个候选 endpoint 并行试探
    - [ ] 明确筛选参数：`customerSegment / status / type / q` 的最终命名与可选值
    - [ ] 明确响应结构统一为 `quote summary list`，避免前端继续猜测字段名
    - [ ] 前端 `quote-list-service` 收口为单一正式接口分支，mock 回退改为受控兜底而不是常态轮询候选路径
    - [ ] 若后端当前尚无正式接口，需要同步补 routes / handlers / services / dto 映射设计
    - [ ] 本轮不扩展到报价详情、CRUD、审批流与转正式销售订单事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 753-报价详情只读接口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 基于已固定的正式报价列表接口，继续新增单一正式 `报价详情只读接口`
    - [ ] 前端新增 `quote detail` 的 contract / adapter / service / hook，保持页面不直接请求 API
    - [ ] 先形成 `列表 -> 详情` 的正式读取闭环，不在本轮直接做编辑、保存与提交流程
    - [ ] 详情结构优先承接后续 `sales-document` 共用模板层所需字段，避免未来再次拆改
    - [ ] 若详情接口暂不可用，可短期保留只读占位态，但不再回到多 endpoint 试探模式
    - [ ] 本轮不扩展报价编辑、审批流与转正式销售订单事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 754-报价详情展示容器升级（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将当前右侧 `详情挂点` 调整为“点击报价打开工作弹窗”的交互形态，弹窗作为单条报价工作台
    - [ ] 弹窗优先承载：详情查看、局部数值编辑、一键保存 PDF、客户微信转发、转正式销售订单等已有/既定动作入口
    - [ ] 在不改详情读取链的前提下，增强头部信息、金额汇总、明细摘要、需求说明等展示分组，并为后续编辑态预留结构
    - [ ] 若需要，为弹窗内容新增独立组件，避免继续把详情展示逻辑堆叠在 `quote-workspace-list.tsx`
    - [ ] 弹窗内容优先对齐后续 `sales-document` 共用模板层的阅读顺序，便于未来从只读切到编辑
    - [ ] 本轮优先落弹窗骨架与动作位，不在本轮直接打通审批流与转正式销售订单完整事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 755-客户联系方式驱动的报价转发动作（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在报价工作弹窗中接入客户真实联系方式字段，至少覆盖 `wechat` 与 `whatsapp`
    - [ ] 若现有报价详情接口未返回联系方式，则补齐后端详情响应字段，并在前端 detail contract / adapter 中同步收口
    - [ ] 转发动作按真实数据自适应显示：有微信则优先显示 `转发微信`，有 WhatsApp 则显示 `转发 WhatsApp`
    - [ ] 若客户未留联系方式，不隐藏动作位，而是保留占位按钮并在点击后明确提示“客户未留联系方式”
    - [ ] 本轮优先接真实联系方式读取与动作文案/可用性切换，不在本轮直接打通微信/WhatsApp 外部发送集成
    - [ ] 本轮不扩展审批流、转正式销售订单完整事务与完整 PDF 流程
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 756-报价真实 PDF 导出（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为报价工作弹窗中的 `一键保存 PDF` 接入真实导出链路，不再停留在静态动作位
    - [ ] 优先复用项目现有打印/预览能力，避免为报价单单独再造第二套导出语义
    - [ ] 先形成 `报价弹窗 -> 预览/打印 -> 保存 PDF` 的闭环，确保现场人员可直接导出给客户
    - [ ] 若现有详情字段不足以支撑导出模板，则在报价详情读取链中最小补齐打印所需字段
    - [ ] 本轮优先完成真实 PDF 导出，不在本轮直接打通微信外部发送与转正式销售订单完整事务
    - [ ] 本轮不扩展审批流与完整编辑保存事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 757-报价弹窗真实保存（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将报价工作弹窗中的最小局部编辑区接入真实保存链，不再停留在前端临时输入态
    - [ ] 优先复用现有 `sales-orders/:id PATCH` 语义，不单独再造第二套 quotes 写入协议
    - [ ] 本轮仅优先打通最小可保存字段：`amount`（对应当前金额编辑区）与 `requirements`
    - [ ] 保存成功后需同步刷新报价详情与列表，避免弹窗与列表摘要数据不一致
    - [ ] 若保存失败，需要在弹窗内给出明确反馈，而不是静默失败
    - [ ] 本轮不扩展完整报价编辑器、不扩展审批流、不扩展转正式销售订单完整事务
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 758-报价转正式销售订单（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将报价工作弹窗中的 `转正式销售订单` 接入真实业务动作，不再停留在静态动作位
    - [ ] 该动作优先作为显式“转单”链路处理，不混入普通 `PATCH` 保存语义
    - [ ] 先明确当前业务语义：是将报价状态改为 `converted`，还是复制/生成新的正式销售订单，再决定接口实现方式
    - [ ] 若现有后端无正式转单接口，则本轮补充单一正式转单入口，并在前端收口到专用 service / hook
    - [ ] 转单成功后需同步刷新报价详情与列表，并视方案决定是否跳转到正式销售订单视图
    - [ ] 转单失败时需给出明确反馈，不可静默失败
    - [ ] 本轮不扩展完整审批流与完整报价编辑器
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 759-报价页删除描述文案并移除 MOCK 回退（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 删除报价工作弹窗右侧“现场高频动作”说明文案，避免干扰真实联调测试
    - [ ] 删除报价链路中所有 mock fallback，尤其是报价列表服务中的 `quoteSummaryMockData` 回退逻辑
    - [ ] 报价列表与详情链路只允许使用真实接口数据，不再用 mock 数据掩盖真实后端问题
    - [ ] 当真实接口失败时，前端需要显示明确错误或空态，而不是自动切换到 mock 数据
    - [ ] 保留真实接口返回的空列表场景，空数据时正常渲染空态，不视为异常
    - [ ] 本轮不扩展新功能，只做文案清理与真实数据链路收口
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 760-报价管理页回归系统 1.0 视觉（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 以系统现有 1.0 风格列表页为参照，重构报价管理页的页头、筛选区、列表区与空态/错误态视觉
    - [ ] 收紧当前过大的留白、过多的标签徽标与偏演示稿的卡片式布局
    - [ ] 将筛选区从“方案展示卡片”收口为更接近系统工具栏/数据表过滤条的结构
    - [ ] 将报价列表区回归系统已有表格容器、表头、行高、边框、状态呈现方式
    - [ ] 将空态与错误态回归表格内态，不再使用偏居中海报式表现
    - [ ] 本轮仅做视觉回归，不扩展新业务功能
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 761-报价页“新建报价”按钮无响应（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 排查报价页筛选区与空态中的 `新建报价` 按钮事件链为何点击无反应
    - [ ] 明确当前缺口是未绑定交互、空实现，还是缺少目标弹窗/路由入口
    - [ ] 若已有可复用的新建单据入口，则优先复用；若尚无完整创建链，则至少补充明确反馈，避免静默无反应
    - [ ] 本轮必须保证点击后有可见结果：打开新建入口，或显示明确提示，不允许继续静默失败
    - [ ] 本轮不扩展完整报价创建后端链路，除非项目内已有稳定入口可直接复用
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 762-复用工作弹窗做新建报价（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 不新增第二个新建弹窗，直接复用现有 `quote-workspace-dialog`
    - [ ] 将工作弹窗扩展为 `create mode / detail mode` 两种模式
    - [ ] 新建模式补充最小必需字段：动态客户选择、金额、需求说明
    - [ ] 新建模式使用空白初始值，不依赖既有报价详情读取
    - [ ] 新建模式下保存按钮语义改为“创建报价”
    - [ ] 新建模式下禁用或隐藏不适用动作：PDF、客户转发、转正式销售订单
    - [ ] 若项目内已有可复用保存链，则优先复用；本轮不新增第二套独立弹窗体系
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 763-报价新建接入完整明细编辑器（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 不再使用“仅客户 + 金额 + 需求说明”的简化表单充当报价新建
    - [ ] 复用项目内成熟的单据头 + 明细表格编辑器能力，接入真实产品选择与明细行编辑
    - [ ] 报价新建至少具备：客户选择、产品选择、明细表格、数量、单价、金额、需求说明
    - [ ] 新建成功后继续留在同一工作弹窗内，原地切入详情态并继续后续动作
    - [ ] 优先复用交易侧已有产品/客户/行编辑能力，避免另起一套报价专用低配编辑器
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 764-报价管理 TAB 职责拆分与稳定性优化（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 拆分 `src/features/quotes/tabs/index.tsx`，将页面编排、create mode 资源加载、创建保存流从单文件中抽离
    - [ ] 为报价工作台新增 controller / adapter 层，隔离 quotes 域与 trading 销售订单编辑能力的直接耦合
    - [ ] 收口 create/detail 两种模式的状态边界，降低弹窗状态污染风险
    - [ ] 控制 `quote-workspace-dialog` 继续膨胀的趋势，优先向 shell + create/detail 内容拆分方向演进
    - [ ] 保持现有可用能力不回退：完整明细编辑器、创建后原地切详情态、现有保存/转单/预览行为
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 765-报价域与 trading 域 adapter/façade 边界收口（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 为 quotes 域新增面向报价工作台的 adapter / façade，避免页面与工作台直接依赖 trading 域的表单与 mutation 语义
    - [ ] 将报价新建所需的表单状态、资源装配、创建提交统一收口到 quotes 域接口中
    - [ ] 降低 `useSalesOrderForm`、`useSalesOrderMutations`、销售单据组件对 quotes 页面的直接暴露程度
    - [ ] 保持现有完整明细编辑能力、创建后原地切详情态、现有保存/转单/预览行为不回退
    - [ ] 优先做轻量防腐层，不复制一套完整 trading 实现
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 766-createResources 并入 quotes create façade（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将客户、产品、单位、打孔、贴标等 create mode 资源装配从 `use-quote-create-editor.ts` 继续下沉到 quotes façade / adapter
    - [ ] 让 quotes create façade 同时承接：表单桥接、资源装配、创建提交
    - [ ] 将 `use-quote-create-editor.ts` 继续收口为更薄的一层报价工作台接口
    - [ ] 保持现有完整明细编辑器和创建后原地切 detail mode 行为不回退
    - [ ] 不复制 trading / engineering / basic-settings 的底层查询实现，只做 quotes 域对外收口
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 767-客户卡片补报价单号入口与报价弹窗联动（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 在客户卡片上显示该客户已有报价的单据号入口，而不是仅在报价页内可见
    - [ ] 若客户存在多张报价，支持在客户卡片侧选择具体报价单号
    - [ ] 选中报价后直接拉起现有报价工作弹窗，复用现有 detail mode 能力
    - [ ] 若客户当前没有报价，在客户卡片上提供明确提醒/警告，并点击后直接拉起现有 create mode 报价弹窗
    - [ ] 不新建第二套报价详情页或独立弹窗实现
    - [ ] 优先通过 quotes 聚合摘要 / 轻量查询为客户卡片提供报价单号列表，避免把客户模块直接耦合进报价详情实现
    - [ ] 执行定向 TypeScript 校验并补充 `walkthrough.md`

- [ ] 768-客户报价摘要升级为后端/独立接口级摘要（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 将客户卡片当前“复用 `/quotes` 列表接口并在前端按客户名二次过滤”的摘要方式升级为独立摘要链路
    - [ ] 由 quotes 后端新增面向客户卡片的轻量摘要接口，仅返回客户卡片所需最小字段
    - [ ] 摘要接口优先按 `customerId` 精确查询，必要时再兼容 `customerName`，避免纯名称匹配带来的歧义
    - [ ] 返回字段控制在最小闭环范围：`quoteId`、`quoteNo`、`status`、`updatedAt`，必要时补充 `customerId`
    - [ ] 前端新增独立 `service/query key/hook` 消费该接口，不再依赖报价列表接口语义
    - [ ] 客户卡片继续复用现有报价工作台打开逻辑，不新增第二套详情/创建实现
    - [ ] 保持 quotes 列表页现有筛选、分页、关键词搜索职责不被客户卡片摘要复用污染
    - [ ] 执行定向后端/前端校验，并补充 `walkthrough.md`

- [ ] 769-auth/snapshot 与登录 502 根因排查与修复（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 定位 `POST /auth/login` 与 `GET /auth/snapshot` 同时返回 502 的共同根因
    - [ ] 优先排查后端认证公共依赖、身份权限快照解析链路与中间件，而不是先在前端增加降级补丁
    - [ ] 重点核查 `LoginHandler` 中的 `ResolveSnapshotForUser(...)` 与 `AuthMiddleware` 中 auth snapshot 装配链路
    - [ ] 明确 502 是后端 panic、数据库/权限依赖异常、还是反向代理转发失败导致
    - [ ] 若根因在后端，优先修复后端并保持 `/auth/login`、`/auth/snapshot` 响应语义稳定
    - [ ] 前端仅在必要时补最小配套，不用网络补丁掩盖后端认证故障
    - [ ] 执行定向后端/前端校验，并补充 `walkthrough.md`

- [ ] 770-客户管理报价记录区“新建报价”按钮引导增强（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 仅优化客户卡片“暂无报价”场景下的“新建报价”按钮显著性
    - [ ] 让按钮在视觉层级上明显高于当前次级 outline 样式
    - [ ] 补充更强的行动引导文案或视觉强调，但不新增第二套流程
    - [ ] 保持点击后仍复用现有报价工作台 `create mode`
    - [ ] 不改已有报价场景下的“打开报价/选择报价”交互
    - [ ] 执行定向 TypeScript 校验，并补充 `walkthrough.md`

- [ ] 771-客户资料编辑保存失败：联系电话修改触发 code/name 为空（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 定位客户编辑保存时 `finalData` 丢失 `code` / `name` 的根因
    - [ ] 核查 `customer-action-dialog`、`buildCustomerSaveSnapshot`、`CustomerList.handleSaveCustomer` 的数据合并顺序
    - [ ] 保证仅修改联系电话等局部字段时，事务保存仍能携带完整必需主数据
    - [ ] 不回退当前客户事务保存链路，只修补 payload 组装缺口
    - [ ] 执行定向 TypeScript 校验，并补充 `walkthrough.md`

- [ ] 772-Engineering：`use-product-form-init.ts` 远端真相归属整改（中文）
  - [ ] 待确认本轮规划范围：
    - [ ] 识别 `use-product-form-init.ts` 中被 `useState + useEffect` 手动托管的远端数据
    - [ ] 将 `attributeCategories`、`attributeOptions`、`attributeBindings` 等服务端真相迁回 React Query
    - [ ] 保留仅属于 UI 的派生选项与表单初始化状态，不把远端缓存再次复制到本地 state
    - [ ] 确保 query 失效或后台数据刷新后，产品表单元数据能响应式更新
    - [ ] 评估并补齐所需 query key / hook 封装，避免在 hook 内直接堆叠请求编排
    - [ ] 执行定向 TypeScript 校验，并补充 `walkthrough.md`
