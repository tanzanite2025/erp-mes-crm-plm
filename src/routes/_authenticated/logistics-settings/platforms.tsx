import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/logistics-settings/platforms')({
  // No redirect - this route now hosts the unified providers tab
})
