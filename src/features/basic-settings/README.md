# 基础设置职责边界

一维码协议 TAB 的跨模块规则以[一维码生命周期契约](../../../docs/architecture/linear-barcode-lifecycle-contract.md)为准。

该 TAB 只负责维护和模拟 15 位协议配置，包括字段定义、显示规则和服务端发号规则键。

该 TAB 不负责真实发号、创建打印批次、逐码库存落库、原材料绑定或库存过期处理。协议配置变化必须继续由服务端作为打印签发的权威输入。
