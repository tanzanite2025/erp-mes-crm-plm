# XDFC 单 VPS 架构分阶段修正方案（先可上线，再可扩展）

> 适用前提：当前仅有 1 台 VITURE VPS，暂无 S3/OSS 预算。  
> 核心目标：**先保证稳定上线**，再逐步修正“伪分布式”问题；所有改造不得阻断现网。

---

## 1. 当前问题基线（按影响优先级）

### P0：部署认知与配置语义不一致
1. `server/docker-compose.yml` 中有 `deploy.replicas: 2`，但在非 Swarm 下通常不生效，容易误判“已多副本”。
2. `server/deployment/nginx/internal_lb.conf` 当前 upstream 只有单后端（`app:8080`），不构成可验证的负载均衡。
3. `nginx.conf.template` 的 API 仍直连 `localhost:8080`，属于单机反向代理语义。

### P1：状态依赖强绑定单机
1. 上传文件写入服务器磁盘目录（`./uploads:/app/uploads`），跨主机不可直接共享。
2. Redis/Postgres 当前部署模型默认单点，后续扩容需要先明确连接与容量策略。

### P2：可观测与容量边界不足
1. 缺少“多实例是否真实分摊”的验证脚本与日常巡检清单。
2. 数据库连接池参数与“目标副本数”未形成预算模型，后续扩容存在踩坑风险。

---

## 2. 约束与原则（必须遵守）

1. **上线优先**：任何阶段都必须保持单 VPS 可运行。
2. **存储现实约束**：当前默认使用 VPS 服务器磁盘，不引入 S3/OSS 硬依赖。
3. **渐进式改造**：先修“假分布式认知问题”，再做“真分布式能力准备”。
4. **可回滚**：每阶段必须有回滚路径，失败可在 30 分钟内恢复。
5. **不做补丁堆叠**：每项改动需绑定根因与验收标准。

---

## 3. 分阶段修正路线

## Phase A（1 周内）：去伪负载均衡 + 单机上线规范化

### 目标
- 解决“看起来是分布式，实际不是”的认知与配置错位。
- 输出一套可重复执行的单 VPS 上线流程。

### 具体动作
1. 明确 Compose 与 Swarm 的语义边界（文档化）：
   - 非 Swarm：使用 `docker compose up -d --scale app=2` 扩副本；
   - `deploy.replicas` 仅保留说明用途，不作为非 Swarm 生效依据。
2. 修正 `internal_lb.conf`：
   - 配置可扩展 upstream（least_conn、超时、失败转移策略）；
   - 增加“如何验证确实分摊”的操作说明。
3. 补充运维检查清单（根目录或 docs）：
   - 启动、扩容、回滚、日志检查、健康检查。

### 验收标准
- 单 VPS 可稳定启动并对外服务。
- 通过演练确认“扩容后请求可分摊”，而非仅“服务可访问”。
- 回滚文档可执行。

### 回滚策略
- 保留旧版 Nginx 配置备份；异常时一键恢复旧配置并重载。

---

## Phase B（2~4 周）：稳定性与容量治理（仍以单 VPS 为主）

### 目标
- 让系统在单 VPS 上具备“可预期容量边界”，避免伪扩容导致连锁故障。

### 具体动作
1. 数据库连接池参数改为可配置（环境变量化），建立预算表：
   - `总连接预算 = 实例数 × 单实例 MaxOpenConns + 运维预留`。
2. Redis 连接配置收敛：
   - 明确单 Redis 中心化连接方式；
   - 输出故障处理手册（密码、重连、超时）。
3. 健康检查与优雅停机核验：
   - 明确探活、启动窗口、重启策略。

### 验收标准
- 压测/演练下无明显连接池打满。
- 重启/滚动时业务可恢复，错误率受控。

### 回滚策略
- 参数变更均支持环境变量回退到前值。

---

## Phase C（4~8 周）：存储解耦准备（不强制切换 S3）

### 目标
- 在不购买 S3/OSS 的前提下，为后续对象存储接入预留接口。

### 具体动作
1. 抽象上传存储接口（local driver 默认启用）。
2. 保持现有 `/uploads/*` URL 兼容，不改现网访问路径。
3. 增加迁移设计：未来接入 S3/OSS 时的双写/回填/切换步骤。

### 验收标准
- 默认仍写 VPS 磁盘，现网无感。
- 代码层已具备可插拔能力，但不引入新外部依赖。

### 回滚策略
- 存储实现默认回退 local driver。

---

## Phase D（资源具备后）：真实分布式落地

### 前置条件
- 至少 2 台应用服务器；
- 可用对象存储（S3/OSS）；
- 明确监控与告警预算。

### 目标
- 形成真正可横向扩展的架构（LB + 多应用实例 + 中央状态服务）。

### 交付物
- `docs/architecture/distributed/distributed-architecture.md`
- 切换计划、回滚计划、故障演练报告。

---

## 4. 与“项目一堆问题”的映射关系

1. “有 replicas 但不一定生效” -> Phase A（语义澄清 + 启动标准化）
2. “LB 只有单上游” -> Phase A（upstream 可扩展 + 验证分摊）
3. “API 直连 localhost” -> Phase A（明确单机与多实例入口策略）
4. “数据库连接可能扩容爆炸” -> Phase B（预算化治理）
5. “上传依赖服务器磁盘” -> Phase C（先抽象，后切换）
6. “未来想真分布式” -> Phase D（资源到位后再实施）

---

## 5. 执行顺序建议（不会阻断上线）

1. 先做 Phase A：修认知 + 修入口 + 出验证清单。  
2. 稳定后做 Phase B：容量与连接治理。  
3. 再做 Phase C：存储可插拔准备（默认仍 VPS 磁盘）。  
4. 最后在资源具备时进入 Phase D：真实分布式。

---

## 6. 本方案的边界声明

- 本方案**不是**立刻改造成分布式系统。  
- 本方案是“单 VPS 现实约束下的最优渐进路径”。  
- 当前阶段不以“炫架构”为目标，以“可上线、可维护、可扩展”为目标。
